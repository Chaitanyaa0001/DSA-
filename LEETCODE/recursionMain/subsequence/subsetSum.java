package LEETCODE.recursionMain.subsequence;

import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class subsetSum {
    // in this we need to print the sum of subsets ad arrrange them in increasing order 
    private static List<Integer> sumOfeachSubset(int nums[], int N){
        List<Integer> ans = new ArrayList<>();
        
        return ans;
    }
    public static void main(String[] args) {
        int arr[] = {3,1,4};
        Scanner sc  = new Scanner(System.in);
        int N = sc.nextInt();
        sumOfeachSubset(arr, N);

    }
}
