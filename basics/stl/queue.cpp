#include<queue>
#include<iostream>

using namespace std;

int main(){

    queue<int> q;
    q.push(2);
    q.pop();
    q.front(); // to return the front elemnt 
    q.size();
    q.empty();
    // q.swap();
    
    return 0;
}