#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;

    while (t --> 0){
        
        int n,k;
        cin >> n >> k;
        // now we are given 2 number n and k 
        // n is the size of matrix and the other  is goal value of set(numbers of elemnt in a set )
        // sum of n  over all test cases is nuder 1000
        
    }
    
}