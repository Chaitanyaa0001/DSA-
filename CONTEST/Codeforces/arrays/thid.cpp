#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t --> 0) {
        long long x, y;
        cin >> x >> y;
        
        long long maxXOR = 0;
        long long minOps = 0;
        
        // Check only critical positions
        for(long long k = 0; k <= 60; k++) {
            // Check around powers of 2
            // ... (optimized logic)
        }
    }
}